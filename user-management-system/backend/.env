# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Local MongoDB (default for local dev)
MONGODB_URI=mongodb+srv://tsotneivanashvili_db_user:cotneiai80@firstproject.vvwopgr.mongodb.net/user_management?retryWrites=true&w=majority&appName=FirstProject



# MongoDB Atlas (replace MONGODB_URI above when ready)
# MONGODB_URI=mongodb+srv://<username>:<url_encoded_password>@<cluster-host>/user_management?retryWrites=true&w=majority&appName=Cluster0

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5500,http://127.0.0.1:3000,http://127.0.0.1:5500
# Add your deployed frontend origin when going live:
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5500,http://127.0.0.1:3000,http://127.0.0.1:5500,https://<your-github-username>.github.io

# Allow file:// frontend origin ("Origin: null") in development
ALLOW_NULL_ORIGIN_IN_DEV=true

# JWT Secret (for future authentication)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

